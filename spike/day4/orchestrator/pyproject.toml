[project]
name = "tally-coding-spike-day4-orchestrator"
version = "0.0.1"
description = "Day 4 spike orchestrator — dispatches coding tasks to workers via Tally Workers"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
    "cryptography>=42.0",
]

[tool.uv]
package = false
