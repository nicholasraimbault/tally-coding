[project]
name = "tally-coding-spike-day4-worker"
version = "0.0.1"
description = "Day 4 spike worker — OpenHands agent driven by Tally Workers wakes"
requires-python = ">=3.12"
dependencies = [
    "openhands-ai",
    "httpx>=0.27.0",
    "cryptography>=42.0",
]

[tool.uv]
package = false
